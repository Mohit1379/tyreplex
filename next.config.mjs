/** @type {import('next').NextConfig} */
const nextConfig = {
        images: {
          domains: ['cdn.tyreplex.net'], // Add the domain here
        },
        // Other configurations (if any)
};

export default nextConfig;
